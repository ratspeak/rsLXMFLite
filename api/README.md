# API fixture

`fixtures/` is a separate Cargo workspace that checks message construction and
validation from a downstream crate. The test matrix compiles it.

Update the fixture when changing the public API.
